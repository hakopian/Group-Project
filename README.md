# Virtual Arcade

Welcome to the Virtual Arcade! This project features a lobby where you can select and play various games.

## Setup

1. Install the required dependencies:
    ```bash
    pip install -r requirements.txt
    ```

2. Run the main script:
    ```bash
    python main.py
    ```
